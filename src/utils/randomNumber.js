function getRandomInt(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

function getColor(
    switch (getRandomInt(1,5)) {
        case value:
            
            break;
    
        default:
            break;
    }
)